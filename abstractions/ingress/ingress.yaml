apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-react-server
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: my-react-server
            port:
              number: 80